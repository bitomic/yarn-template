name: Node.js CI

on:
  push:
    paths:
      - '.github/workflows/node.js.yml'
      - 'src/**'
      - 'Dockerfile'
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/node.js.yml'
      - 'src/**'
      - 'Dockerfile'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    uses: bitomic/utilities/.github/workflows/docker-build.yml@07e0fabfcd1d7a386f95c7ec698eb221418295ca
