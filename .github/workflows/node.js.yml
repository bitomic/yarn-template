name: Node.js CI

on:
  push:
    paths:
      - '.github/workflows/node.js.yml'
      - 'src/**'
      - 'Dockerfile'
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/node.js.yml'
      - 'src/**'
      - 'Dockerfile'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    uses: bitomic/utilities/.github/workflows/docker-build.yml@24e16be2f54403d1e5a3d6ddba43b73642adac61
